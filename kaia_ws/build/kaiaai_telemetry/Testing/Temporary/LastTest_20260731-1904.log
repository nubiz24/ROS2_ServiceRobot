Start testing: Aug 01 02:04 +07
----------------------------------------------------------
1/4 Testing: cppcheck
1/4 Test: cppcheck
Command: "/usr/bin/python3.10" "-u" "/opt/ros/iron/share/ament_cmake_test/cmake/run_test.py" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/cppcheck.xunit.xml" "--package-name" "kaiaai_telemetry" "--output-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/ament_cppcheck/cppcheck.txt" "--command" "/opt/ros/iron/bin/ament_cppcheck" "--xunit-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/cppcheck.xunit.xml"
Directory: /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry
"cppcheck" start time: Aug 01 02:04 +07
Output:
----------------------------------------------------------
-- run_test.py: invoking following command in '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry':
 - /opt/ros/iron/bin/ament_cppcheck --xunit-file /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/cppcheck.xunit.xml
cppcheck 2.7 has known performance issues and therefore will not be used, set the AMENT_CPPCHECK_ALLOW_SLOW_VERSIONS environment variable to override this.
-- run_test.py: return code 0
-- run_test.py: verify result file '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/cppcheck.xunit.xml'
<end of output>
Test time =   0.28 sec
----------------------------------------------------------
Test Passed.
"cppcheck" end time: Aug 01 02:04 +07
"cppcheck" time elapsed: 00:00:00
----------------------------------------------------------

2/4 Testing: lint_cmake
2/4 Test: lint_cmake
Command: "/usr/bin/python3.10" "-u" "/opt/ros/iron/share/ament_cmake_test/cmake/run_test.py" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/lint_cmake.xunit.xml" "--package-name" "kaiaai_telemetry" "--output-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/ament_lint_cmake/lint_cmake.txt" "--command" "/opt/ros/iron/bin/ament_lint_cmake" "--xunit-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/lint_cmake.xunit.xml"
Directory: /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry
"lint_cmake" start time: Aug 01 02:04 +07
Output:
----------------------------------------------------------
-- run_test.py: invoking following command in '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry':
 - /opt/ros/iron/bin/ament_lint_cmake --xunit-file /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/lint_cmake.xunit.xml

No problems found
-- run_test.py: return code 0
-- run_test.py: verify result file '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/lint_cmake.xunit.xml'
<end of output>
Test time =   0.24 sec
----------------------------------------------------------
Test Passed.
"lint_cmake" end time: Aug 01 02:04 +07
"lint_cmake" time elapsed: 00:00:00
----------------------------------------------------------

3/4 Testing: uncrustify
3/4 Test: uncrustify
Command: "/usr/bin/python3.10" "-u" "/opt/ros/iron/share/ament_cmake_test/cmake/run_test.py" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/uncrustify.xunit.xml" "--package-name" "kaiaai_telemetry" "--output-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/ament_uncrustify/uncrustify.txt" "--command" "/opt/ros/iron/bin/ament_uncrustify" "--xunit-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/uncrustify.xunit.xml"
Directory: /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry
"uncrustify" start time: Aug 01 02:04 +07
Output:
----------------------------------------------------------
-- run_test.py: invoking following command in '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry':
 - /opt/ros/iron/bin/ament_uncrustify --xunit-file /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/uncrustify.xunit.xml
Code style divergence in file 'src/lds.h':

--- src/lds.h
+++ src/lds.h.uncrustify
@@ -26,2 +26,2 @@
-  typedef void (*ScanPointCallback)(const void * context, float, float, float, float, bool);
-  typedef int (*ReadByteCallback)(const void * context);
+  typedef void (* ScanPointCallback)(const void * context, float, float, float, float, bool);
+  typedef int (* ReadByteCallback)(const void * context);
@@ -29 +29,2 @@
-  enum result_t {
+  enum result_t
+  {
@@ -49 +50,2 @@
-  virtual ~LDS() {}
+  virtual ~LDS() {
+  }
@@ -55 +57,2 @@
-  void setScanPointCallback(ScanPointCallback scan_point_callback) {
+  void setScanPointCallback(ScanPointCallback scan_point_callback)
+  {
@@ -59 +62,2 @@
-  void setReadByteCallback(ReadByteCallback read_byte_callback) {
+  void setReadByteCallback(ReadByteCallback read_byte_callback)
+  {
@@ -64,2 +68,4 @@
-  void postScanPoint(const void * context, float angle_deg, float dist_mm,
-    float quality, float intensity_lsb, bool scan_completed) {
+  void postScanPoint(
+    const void * context, float angle_deg, float dist_mm,
+    float quality, float intensity_lsb, bool scan_completed)
+  {
@@ -67 +73 @@
-    if (scan_point_callback)
+    if (scan_point_callback) {
@@ -68,0 +75 @@
+    }
@@ -71 +78,2 @@
-  int readByte(const void * context) {
+  int readByte(const void * context)
+  {

Code style divergence in file 'src/lds_camsense_x1.h':

--- src/lds_camsense_x1.h
+++ src/lds_camsense_x1.h.uncrustify
@@ -18 +18 @@
-class LDS_CamsenseX1 : public LDS
+class LDS_CamsenseX1: public LDS
@@ -27 +27,2 @@
-  struct meas_sample_t {
+  struct meas_sample_t
+  {
@@ -32 +33,2 @@
-  struct scan_packet_t {
+  struct scan_packet_t
+  {
@@ -57 +59 @@
-  static const std::string get_model_name() { return "CAMSENSE-X1"; }
+  static const std::string get_model_name() {return "CAMSENSE-X1";}
@@ -59 +61,2 @@
-  virtual float get_scan_time() override {
+  virtual float get_scan_time() override
+  {
@@ -63 +66,2 @@
-  uint16_t decodeUInt16(const uint16_t value) const {
+  uint16_t decodeUInt16(const uint16_t value) const
+  {
@@ -77 +81 @@
-    if (current_byte < 0)
+    if (current_byte < 0) {
@@ -78,0 +83 @@
+    }
@@ -91,2 +96,92 @@
-    case 1:
-      if (c != START_BYTE0) {
+      case 1:
+        if (c != START_BYTE0) {
+          parser_idx = 0;
+        }
+        break;
+
+      case 2:
+        if (c != START_BYTE1) {
+          parser_idx = 0;
+        }
+        break;
+
+      case 3:
+        if (c != START_BYTE2) {
+          parser_idx = 0;
+        }
+        break;
+
+      case 4:
+        if (c != SAMPLES_PER_PACKET) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 5: // speed LSB
+      case 6: // speed MSB
+        break;
+
+      case 7: // start angle LSB
+      case 8: // start angle MSB
+        break;
+
+      default:
+        if (parser_idx > sizeof(scan_packet_t)) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case sizeof(scan_packet_t) - 3: // end angle LSB
+      case sizeof(scan_packet_t) - 2: // end angle MSB
+        break;
+
+      case sizeof(scan_packet_t) - 1: // CRC16 LSB
+        break;
+
+      case sizeof(scan_packet_t) - 0: // CRC16 MSB
+
+        rotation_speed = decodeUInt16(scan_packet.rotation_speed);
+        uint16_t start_angle = decodeUInt16(scan_packet.start_angle);
+        uint16_t end_angle = decodeUInt16(scan_packet.end_angle);
+
+        if (start_angle < ANGLE_MIN || end_angle < ANGLE_MIN) {
+          result = RESULT_INVALID_PACKET;
+          break;
+        }
+
+        bool scan_completed_mid_packet = end_angle < start_angle;
+        bool scan_completed_between_packets = start_angle < end_angle_prev;
+        bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
+        end_angle_prev = end_angle;
+
+        static float constexpr ONE_OVER_64 = 1.0 / 64;
+        float start_angle_deg = (start_angle - ANGLE_MIN) * ONE_OVER_64;
+        float end_angle_deg = (end_angle - ANGLE_MIN) * ONE_OVER_64;
+        if (start_angle > end_angle) {
+          end_angle_deg += 360;
+        }
+
+        static float constexpr ONE_OVER_7 = 1.0 / (SAMPLES_PER_PACKET - 1);
+        float step_deg = end_angle_deg - start_angle_deg;
+        step_deg *= ONE_OVER_7;
+
+        float angle_deg_prev = start_angle_deg;
+        for (uint8_t i = 0; i < SAMPLES_PER_PACKET; i++) {
+          float distance_mm = (int16_t) decodeUInt16(scan_packet.sample[i].distance_mm);
+          float quality = scan_packet.sample[i].quality;
+
+          float angle_deg = start_angle_deg + step_deg * i;
+
+          scan_completed = false;
+          if (scan_completed_mid_packet) {
+            scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
+          } else if (scan_completed_between_packets) {
+            scan_completed = (i == 0);
+          }
+          angle_deg_prev = angle_deg;
+
+          angle_deg = angle_deg > 360 ? angle_deg - 360 : angle_deg;
+          distance_mm = distance_mm < 0 ? 0 : distance_mm;
+
+          postScanPoint(context, angle_deg, distance_mm, quality, 0, scan_completed);
+        }
@@ -94,50 +188,0 @@
-      }
-      break;
-
-    case 2:
-      if (c != START_BYTE1) {
-        parser_idx = 0;
-      }
-      break;
-
-    case 3:
-      if (c != START_BYTE2) {
-        parser_idx = 0;
-      }
-      break;
-
-    case 4:
-      if (c != SAMPLES_PER_PACKET) {
-        result = RESULT_INVALID_PACKET;
-      }
-      break;
-
-    case 5: // speed LSB
-    case 6: // speed MSB
-      break;
-
-    case 7: // start angle LSB
-    case 8: // start angle MSB
-      break;
-
-    default:
-      if (parser_idx > sizeof(scan_packet_t)) {
-        result = RESULT_INVALID_PACKET;
-      }
-      break;
-
-    case sizeof(scan_packet_t) - 3: // end angle LSB
-    case sizeof(scan_packet_t) - 2: // end angle MSB
-      break;
-
-    case sizeof(scan_packet_t) - 1: // CRC16 LSB
-      break;
-
-    case sizeof(scan_packet_t) - 0: // CRC16 MSB
-
-      rotation_speed = decodeUInt16(scan_packet.rotation_speed);
-      uint16_t start_angle = decodeUInt16(scan_packet.start_angle);
-      uint16_t end_angle = decodeUInt16(scan_packet.end_angle);
-
-      if (start_angle < ANGLE_MIN || end_angle < ANGLE_MIN) {
-        result = RESULT_INVALID_PACKET;
@@ -145,39 +189,0 @@
-      }
-
-      bool scan_completed_mid_packet = end_angle < start_angle;
-      bool scan_completed_between_packets = start_angle < end_angle_prev;
-      bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
-      end_angle_prev = end_angle;
-
-      static float constexpr ONE_OVER_64 = 1.0 / 64;
-      float start_angle_deg = (start_angle - ANGLE_MIN) * ONE_OVER_64;
-      float end_angle_deg = (end_angle - ANGLE_MIN) * ONE_OVER_64;
-      if (start_angle > end_angle)
-        end_angle_deg += 360;
-
-      static float constexpr ONE_OVER_7 = 1.0 / (SAMPLES_PER_PACKET - 1);
-      float step_deg = end_angle_deg - start_angle_deg;
-      step_deg *= ONE_OVER_7;
-
-      float angle_deg_prev = start_angle_deg;
-      for (uint8_t i = 0; i < SAMPLES_PER_PACKET; i++) {
-        float distance_mm = (int16_t) decodeUInt16(scan_packet.sample[i].distance_mm);
-        float quality = scan_packet.sample[i].quality;
-
-        float angle_deg = start_angle_deg + step_deg * i;
-
-        scan_completed = false;
-        if (scan_completed_mid_packet) {
-          scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
-        } else if (scan_completed_between_packets) {
-          scan_completed = (i == 0);
-        }
-        angle_deg_prev = angle_deg;
-
-        angle_deg = angle_deg > 360 ? angle_deg - 360 : angle_deg;
-        distance_mm = distance_mm < 0 ? 0 : distance_mm;
-
-        postScanPoint(context, angle_deg, distance_mm, quality, 0, scan_completed);
-      }
-      parser_idx = 0;
-      break;
@@ -186 +192 @@
-    if (result < RESULT_OK)
+    if (result < RESULT_OK) {
@@ -187,0 +194 @@
+    }

Code style divergence in file 'src/lds_delta_2a.h':

--- src/lds_delta_2a.h
+++ src/lds_delta_2a.h.uncrustify
@@ -18 +18 @@
-class LDS_Delta2A : public LDS
+class LDS_Delta2A: public LDS
@@ -28 +28,2 @@
-  struct meas_sample_t {
+  struct meas_sample_t
+  {
@@ -34,6 +35,7 @@
-  struct scan_packet_t {
-    uint8_t    start_byte; // 0xAA
-    uint16_t   packet_length;
-    uint8_t    protocol_version; // 0x01
-    uint8_t    packet_type; // 0x61
-    uint8_t    data_type; // 0xAE -> data_length -> scan_freq_x20 -> no data
+  struct scan_packet_t
+  {
+    uint8_t start_byte;    // 0xAA
+    uint16_t packet_length;
+    uint8_t protocol_version;    // 0x01
+    uint8_t packet_type;    // 0x61
+    uint8_t data_type;    // 0xAE -> data_length -> scan_freq_x20 -> no data
@@ -41,2 +43,2 @@
-    uint16_t   data_length; // n_samples = (data_length - 5)/3;
-    uint8_t    scan_freq_x20;
+    uint16_t data_length;   // n_samples = (data_length - 5)/3;
+    uint8_t scan_freq_x20;
@@ -48 +50 @@
-    uint16_t   checksum;
+    uint16_t checksum;
@@ -51 +53,2 @@
-  virtual uint8_t get_packets_per_scan() {
+  virtual uint8_t get_packets_per_scan()
+  {
@@ -69,3 +72,4 @@
-  static const std::string get_model_name() { return "3IROBOTIX-DELTA-2A"; }
-
-  virtual float get_scan_time() override {
+  static const std::string get_model_name() {return "3IROBOTIX-DELTA-2A";}
+
+  virtual float get_scan_time() override
+  {
@@ -75 +79,2 @@
-  uint16_t decodeUInt16(const uint16_t value) const {
+  uint16_t decodeUInt16(const uint16_t value) const
+  {
@@ -91 +96 @@
-    if (current_byte < 0)
+    if (current_byte < 0) {
@@ -92,0 +98 @@
+    }
@@ -107,2 +113,93 @@
-    case 1:
-      if (c != START_BYTE) {
+      case 1:
+        if (c != START_BYTE) {
+          parser_idx = 0;
+          result = RESULT_OK; //RESULT_INVALID_PACKET;
+        } else {
+          checksum = c;
+        }
+        break;
+
+      case 2:
+        break;
+
+      case 3:
+        packet_length = decodeUInt16(scan_packet.packet_length);
+        if (packet_length > sizeof(scan_packet_t) - sizeof(scan_packet.checksum)) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 4:
+        if (c != PROTOCOL_VERSION) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 5:
+        if (c != PACKET_TYPE) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 6:
+        if (c != DATA_TYPE_RPM_AND_MEAS && c != DATA_TYPE_RPM_ONLY) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 7: // data length MSB
+        break;
+
+      case 8: // data length LSB
+        data_length = decodeUInt16(scan_packet.data_length);
+        if (data_length == 0 || data_length > MAX_DATA_BYTE_LEN) {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      default:
+        // Keep reading
+        packet_length = decodeUInt16(scan_packet.packet_length);
+        if (parser_idx != packet_length + 2) {
+          break;
+        }
+
+        uint16_t pkt_checksum = (rx_buffer[parser_idx - 2] << 8) + rx_buffer[parser_idx - 1];
+
+        pkt_checksum += rx_buffer[parser_idx - 2];
+        pkt_checksum += rx_buffer[parser_idx - 1];
+        if (checksum != pkt_checksum) {
+          result = RESULT_CHECKSUM_ERROR;
+          break;
+        }
+
+        scan_freq_hz = scan_packet.scan_freq_x20 * 0.05;
+
+        if (scan_packet.data_type == DATA_TYPE_RPM_AND_MEAS) {
+          uint16_t start_angle_x100 = decodeUInt16(scan_packet.start_angle_x100);
+          bool scan_completed = start_angle_x100 == 0;
+
+          data_length = decodeUInt16(scan_packet.data_length);
+          if (data_length < 8) {
+            result = RESULT_INVALID_PACKET;
+            break;
+          }
+
+          uint16_t sample_count = (data_length - 5) / 3;
+          if (sample_count > MAX_DATA_SAMPLES) {
+            result = RESULT_INVALID_PACKET;
+            break;
+          }
+          float start_angle = start_angle_x100 * 0.01;
+          float coeff = 360.0f / (get_packets_per_scan() * sample_count);
+
+          for (uint16_t idx = 0; idx < sample_count; idx++) {
+            float angle_deg = start_angle + idx * coeff;
+
+            uint16_t distance_mm_x4 = decodeUInt16(scan_packet.sample[idx].distance_mm_x4);
+            float distance_mm = distance_mm_x4 * 0.25;
+            float quality = scan_packet.sample[idx].quality;
+            postScanPoint(context, angle_deg, distance_mm, quality, 0, scan_completed);
+            scan_completed = false;
+          }
+        }
@@ -110,83 +207,4 @@
-        result = RESULT_OK; //RESULT_INVALID_PACKET;
-      } else
-        checksum = c;
-      break;
-
-    case 2:
-      break;
-
-    case 3:
-      packet_length = decodeUInt16(scan_packet.packet_length);
-      if (packet_length > sizeof(scan_packet_t) - sizeof(scan_packet.checksum))
-        result = RESULT_INVALID_PACKET;
-      break;
-
-    case 4:
-      if (c != PROTOCOL_VERSION)
-        result = RESULT_INVALID_PACKET;
-      break;
-
-    case 5:
-      if (c != PACKET_TYPE)
-        result = RESULT_INVALID_PACKET;
-      break;
-
-    case 6:
-      if (c != DATA_TYPE_RPM_AND_MEAS && c != DATA_TYPE_RPM_ONLY)
-        result = RESULT_INVALID_PACKET;
-      break;
-
-    case 7: // data length MSB
-      break;
-
-    case 8: // data length LSB
-      data_length = decodeUInt16(scan_packet.data_length);
-      if (data_length == 0 || data_length > MAX_DATA_BYTE_LEN)
-        result = RESULT_INVALID_PACKET;
-      break;
-
-    default:
-      // Keep reading
-      packet_length = decodeUInt16(scan_packet.packet_length);
-      if (parser_idx != packet_length + 2)
-        break;
-
-      uint16_t pkt_checksum = (rx_buffer[parser_idx-2] << 8) + rx_buffer[parser_idx-1];
-
-      pkt_checksum += rx_buffer[parser_idx-2];
-      pkt_checksum += rx_buffer[parser_idx-1];
-      if (checksum != pkt_checksum) {
-        result = RESULT_CHECKSUM_ERROR;
-        break;
-      }
-
-      scan_freq_hz = scan_packet.scan_freq_x20 * 0.05;
-
-      if (scan_packet.data_type == DATA_TYPE_RPM_AND_MEAS) {
-        uint16_t start_angle_x100 = decodeUInt16(scan_packet.start_angle_x100);
-        bool scan_completed = start_angle_x100 == 0;
-
-        data_length = decodeUInt16(scan_packet.data_length);
-        if (data_length < 8) {
-          result = RESULT_INVALID_PACKET;
-          break;
-        }
-
-        uint16_t sample_count = (data_length - 5) / 3;
-        if (sample_count > MAX_DATA_SAMPLES) {
-          result = RESULT_INVALID_PACKET;
-          break;
-        }
-        float start_angle = start_angle_x100 * 0.01;
-        float coeff = 360.0f / (get_packets_per_scan() * sample_count);
-
-        for (uint16_t idx = 0; idx < sample_count; idx++) {
-          float angle_deg = start_angle + idx * coeff;
-
-          uint16_t distance_mm_x4 = decodeUInt16(scan_packet.sample[idx].distance_mm_x4);
-          float distance_mm = distance_mm_x4 * 0.25;
-          float quality = scan_packet.sample[idx].quality;
-          postScanPoint(context, angle_deg, distance_mm, quality, 0, scan_completed);
-          scan_completed = false;
-        }
-      }
+        break;
+    }
+
+    if (result < RESULT_OK) {
@@ -194,5 +212 @@
-      break;
-    }
-
-    if (result < RESULT_OK)
-      parser_idx = 0;
+    }

Code style divergence in file 'src/lds_delta_2b.h':

--- src/lds_delta_2b.h
+++ src/lds_delta_2b.h.uncrustify
@@ -18 +18 @@
-class LDS_Delta2B : public LDS_Delta2A
+class LDS_Delta2B: public LDS_Delta2A
@@ -21,2 +21,3 @@
-  LDS_Delta2B() : LDS_Delta2A() {}
-  static const std::string get_model_name() { return "3IROBOTIX-DELTA-2B"; }
+  LDS_Delta2B() : LDS_Delta2A() {
+  }
+  static const std::string get_model_name() {return "3IROBOTIX-DELTA-2B";}

Code style divergence in file 'src/lds_delta_2g.h':

--- src/lds_delta_2g.h
+++ src/lds_delta_2g.h.uncrustify
@@ -18 +18 @@
-class LDS_Delta2G : public LDS_Delta2A
+class LDS_Delta2G: public LDS_Delta2A
@@ -21,3 +21,5 @@
-  LDS_Delta2G() : LDS_Delta2A() {}
-  static const std::string get_model_name() { return "3IROBOTIX-DELTA-2G"; }
-  uint8_t get_packets_per_scan() override {
+  LDS_Delta2G() : LDS_Delta2A() {
+  }
+  static const std::string get_model_name() {return "3IROBOTIX-DELTA-2G";}
+  uint8_t get_packets_per_scan() override
+  {

Code style divergence in file 'src/lds_ldrobot_ld14p.h':

--- src/lds_ldrobot_ld14p.h
+++ src/lds_ldrobot_ld14p.h.uncrustify
@@ -18 +18 @@
-class LDS_LDRobotLD14P : public LDS
+class LDS_LDRobotLD14P: public LDS
@@ -25 +25,2 @@
-  struct meas_sample_t {
+  struct meas_sample_t
+  {
@@ -32 +33,2 @@
-  struct scan_packet_t {
+  struct scan_packet_t
+  {
@@ -58,3 +60,4 @@
-  static const std::string get_model_name() { return "LDROBOT-LD14P"; }
-
-  virtual float get_scan_time() override {
+  static const std::string get_model_name() {return "LDROBOT-LD14P";}
+
+  virtual float get_scan_time() override
+  {
@@ -64 +67,2 @@
-  uint16_t decodeUInt16(const uint16_t value) const {
+  uint16_t decodeUInt16(const uint16_t value) const
+  {
@@ -73 +77,2 @@
-  void checkSum(uint8_t value) {
+  void checkSum(uint8_t value)
+  {
@@ -106 +111 @@
-    if (current_byte < 0)
+    if (current_byte < 0) {
@@ -107,0 +113 @@
+    }
@@ -119 +125 @@
-    if (parser_idx < 47)
+    if (parser_idx < 47) {
@@ -120,0 +127 @@
+    }
@@ -123,3 +130,100 @@
-    case 1:
-      if (c != START_BYTE) {
-        //result = ERROR_INVALID_VALUE;
+      case 1:
+        if (c != START_BYTE) {
+          //result = ERROR_INVALID_VALUE;
+          parser_idx = 0;
+        }
+        break;
+
+      case 2:
+        if (c != VER_LEN) {
+          //result = ERROR_INVALID_VALUE;
+          parser_idx = 0;
+        }
+        break;
+
+      case 3: // speed LSB
+      case 4: // speed MSB
+        break;
+
+      case 5: // start angle LSB
+      case 6: // start angle MSB
+        break;
+
+      default:
+        if (parser_idx > 6 && parser_idx <= (6 + DATA_BYTE_LEN)) {
+        } else {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 43: // end angle LSB
+      case 44: // end angle MSB
+        break;
+
+      case 45: // timestamp LSB
+      case 46: // end angle MSB
+        break;
+
+      case 47: // CRC
+        if (crc != scan_packet.crc8) {
+          result = RESULT_CHECKSUM_ERROR;
+        } else {
+
+          speed_deg_per_sec = decodeUInt16(scan_packet.speed_deg_per_sec);
+          uint16_t start_angle_deg_x100 = decodeUInt16(scan_packet.start_angle_deg_x100);
+          uint16_t end_angle_deg_x100 = decodeUInt16(scan_packet.end_angle_deg_x100);
+
+          bool scan_completed_mid_packet = end_angle_deg_x100 < start_angle_deg_x100;
+          bool scan_completed_between_packets = start_angle_deg_x100 < end_angle_deg_x100_prev;
+          bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
+          end_angle_deg_x100_prev = end_angle_deg_x100;
+
+          float start_angle = start_angle_deg_x100 * 0.01f;
+          float end_angle = end_angle_deg_x100 * 0.01f;
+
+          if (end_angle < start_angle) {
+            end_angle = end_angle + 360;
+          }
+
+          static constexpr float _1_OVER_PPP_M1 = 1.0f / (POINTS_PER_PACK - 1);
+          float step_angle = (end_angle - start_angle) * _1_OVER_PPP_M1;
+
+          float angle_deg_prev = start_angle;
+          float last_shift_delta = 0;
+          const float RAD_TO_DEG = 57.295779513082320876798154814105f;
+          for (uint8_t i = 0; i < POINTS_PER_PACK; i++) {
+            float angle_deg = start_angle + step_angle * i;
+            float distance_mm = decodeUInt16(scan_packet.sample[i].distance_mm);
+            float quality = scan_packet.sample[i].intensity;
+
+            float offset_x_ = 5.9f;
+            float offset_y_ = -18.975571f;
+            float angle_corrected;
+            if (distance_mm > 0) {
+              float x = distance_mm + offset_x_;
+              float y = distance_mm * 0.11923f + offset_y_;
+              float shift = atan(y / x) * RAD_TO_DEG;
+              angle_corrected = angle_deg - shift;
+              last_shift_delta = shift;
+            } else {
+              angle_corrected = angle_deg - last_shift_delta;
+            }
+
+            scan_completed = false;
+            if (scan_completed_mid_packet) {
+              scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
+            } else if (scan_completed_between_packets) {
+              scan_completed = (i == 0);
+            }
+
+            if (angle_corrected > 360) {
+              angle_corrected -= 360;
+            }
+            if (angle_corrected < 0) {
+              angle_corrected += 360;
+            }
+
+            postScanPoint(context, angle_corrected, distance_mm, quality, 0, scan_completed);
+            angle_deg_prev = angle_deg;
+          }
+        }
@@ -127,93 +231,4 @@
-      }
-      break;
-
-    case 2:
-      if (c != VER_LEN) {
-        //result = ERROR_INVALID_VALUE;
-        parser_idx = 0;
-      }
-      break;
-
-    case 3: // speed LSB
-    case 4: // speed MSB
-      break;
-
-    case 5: // start angle LSB
-    case 6: // start angle MSB
-      break;
-
-    default:
-      if (parser_idx > 6 && parser_idx <= (6 + DATA_BYTE_LEN)) {
-      } else {
-        result = RESULT_INVALID_PACKET;
-      }
-      break;
-
-    case 43: // end angle LSB
-    case 44: // end angle MSB
-      break;
-
-    case 45: // timestamp LSB
-    case 46: // end angle MSB
-      break;
-
-    case 47: // CRC
-      if (crc != scan_packet.crc8) {
-        result = RESULT_CHECKSUM_ERROR;
-      } else {
-
-        speed_deg_per_sec = decodeUInt16(scan_packet.speed_deg_per_sec);
-        uint16_t start_angle_deg_x100 = decodeUInt16(scan_packet.start_angle_deg_x100);
-        uint16_t end_angle_deg_x100 = decodeUInt16(scan_packet.end_angle_deg_x100);
-
-        bool scan_completed_mid_packet = end_angle_deg_x100 < start_angle_deg_x100;
-        bool scan_completed_between_packets = start_angle_deg_x100 < end_angle_deg_x100_prev;
-        bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
-        end_angle_deg_x100_prev = end_angle_deg_x100;
-
-        float start_angle = start_angle_deg_x100*0.01f;
-        float end_angle = end_angle_deg_x100*0.01f;
-
-        if (end_angle < start_angle)
-          end_angle = end_angle + 360;
-
-        static constexpr float _1_OVER_PPP_M1 = 1.0f / (POINTS_PER_PACK - 1);
-        float step_angle = (end_angle - start_angle)*_1_OVER_PPP_M1;
-
-        float angle_deg_prev = start_angle;
-        float last_shift_delta = 0;
-        const float RAD_TO_DEG = 57.295779513082320876798154814105f;
-        for (uint8_t i = 0; i < POINTS_PER_PACK; i++) {
-          float angle_deg = start_angle + step_angle*i;
-          float distance_mm = decodeUInt16(scan_packet.sample[i].distance_mm);
-          float quality = scan_packet.sample[i].intensity;
-
-          float offset_x_ = 5.9f;
-          float offset_y_ = -18.975571f;
-          float angle_corrected;
-          if (distance_mm > 0) {
-            float x = distance_mm + offset_x_;
-            float y = distance_mm * 0.11923f + offset_y_;
-            float shift = atan(y / x) * RAD_TO_DEG;
-            angle_corrected = angle_deg - shift;
-            last_shift_delta = shift;
-          } else {
-            angle_corrected = angle_deg - last_shift_delta;
-          }
-
-          scan_completed = false;
-          if (scan_completed_mid_packet) {
-            scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
-          } else if (scan_completed_between_packets) {
-            scan_completed = (i == 0);
-          }
-
-          if (angle_corrected > 360)
-            angle_corrected -= 360;
-          if (angle_corrected < 0)
-            angle_corrected += 360;
-
-          postScanPoint(context, angle_corrected, distance_mm, quality, 0, scan_completed);
-          angle_deg_prev = angle_deg;
-        }
-      }
+        break;
+    }
+
+    if (result < RESULT_OK) {
@@ -221,5 +236 @@
-      break;
-    }
-
-    if (result < RESULT_OK)
-      parser_idx = 0;
+    }

Code style divergence in file 'src/lds_ldrobot_ld19.h':

--- src/lds_ldrobot_ld19.h
+++ src/lds_ldrobot_ld19.h.uncrustify
@@ -18 +18 @@
-class LDS_LDRobotLD19 : public LDS
+class LDS_LDRobotLD19: public LDS
@@ -25 +25,2 @@
-  struct meas_sample_t {
+  struct meas_sample_t
+  {
@@ -32 +33,2 @@
-  struct scan_packet_t {
+  struct scan_packet_t
+  {
@@ -58,3 +60,4 @@
-  static const std::string get_model_name() { return "LDROBOT-LD19"; }
-
-  virtual float get_scan_time() override {
+  static const std::string get_model_name() {return "LDROBOT-LD19";}
+
+  virtual float get_scan_time() override
+  {
@@ -64 +67,2 @@
-  uint16_t decodeUInt16(const uint16_t value) const {
+  uint16_t decodeUInt16(const uint16_t value) const
+  {
@@ -73 +77,2 @@
-  void checkSum(uint8_t value) {
+  void checkSum(uint8_t value)
+  {
@@ -106 +111 @@
-    if (current_byte < 0)
+    if (current_byte < 0) {
@@ -107,0 +113 @@
+    }
@@ -119 +125 @@
-    if (parser_idx < 47)
+    if (parser_idx < 47) {
@@ -120,0 +127 @@
+    }
@@ -123,3 +130,88 @@
-    case 1:
-      if (c != START_BYTE) {
-        //result = ERROR_INVALID_VALUE;
+      case 1:
+        if (c != START_BYTE) {
+          //result = ERROR_INVALID_VALUE;
+          parser_idx = 0;
+        }
+        break;
+
+      case 2:
+        if (c != VER_LEN) {
+          //result = ERROR_INVALID_VALUE;
+          parser_idx = 0;
+        }
+        break;
+
+      case 3: // speed LSB
+      case 4: // speed MSB
+        break;
+
+      case 5: // start angle LSB
+      case 6: // start angle MSB
+        break;
+
+      default:
+        if (parser_idx > 6 && parser_idx <= (6 + DATA_BYTE_LEN)) {
+        } else {
+          result = RESULT_INVALID_PACKET;
+        }
+        break;
+
+      case 43: // end angle LSB
+      case 44: // end angle MSB
+        break;
+
+      case 45: // timestamp LSB
+      case 46: // end angle MSB
+        break;
+
+      case 47: // CRC
+        if (crc != scan_packet.crc8) {
+          result = RESULT_CHECKSUM_ERROR;
+        } else {
+
+          speed_deg_per_sec = decodeUInt16(scan_packet.speed_deg_per_sec);
+          uint16_t start_angle_deg_x100 = decodeUInt16(scan_packet.start_angle_deg_x100);
+          uint16_t end_angle_deg_x100 = decodeUInt16(scan_packet.end_angle_deg_x100);
+
+          bool scan_completed_mid_packet = end_angle_deg_x100 < start_angle_deg_x100;
+          bool scan_completed_between_packets = start_angle_deg_x100 < end_angle_deg_x100_prev;
+          bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
+          end_angle_deg_x100_prev = end_angle_deg_x100;
+
+          float start_angle = start_angle_deg_x100 * 0.01f;
+          float end_angle = end_angle_deg_x100 * 0.01f;
+
+          if (end_angle < start_angle) {
+            end_angle = end_angle + 360;
+          }
+
+          static constexpr float _1_OVER_PPP_M1 = 1.0f / (POINTS_PER_PACK - 1);
+          float step_angle = (end_angle - start_angle) * _1_OVER_PPP_M1;
+
+          float angle_deg_prev = start_angle;
+          for (uint8_t i = 0; i < POINTS_PER_PACK; i++) {
+            float angle_deg = start_angle + step_angle * i;
+            float distance_mm = decodeUInt16(scan_packet.sample[i].distance_mm);
+            float quality = scan_packet.sample[i].intensity;
+
+            // no correction applied
+            float angle_corrected = angle_deg;
+
+            scan_completed = false;
+            if (scan_completed_mid_packet) {
+              scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
+            } else if (scan_completed_between_packets) {
+              scan_completed = (i == 0);
+            }
+
+            if (angle_corrected > 360) {
+              angle_corrected -= 360;
+            }
+            if (angle_corrected < 0) {
+              angle_corrected += 360;
+            }
+
+            postScanPoint(context, angle_corrected, distance_mm, quality, 0, scan_completed);
+            angle_deg_prev = angle_deg;
+          }
+        }
@@ -127,81 +219,4 @@
-      }
-      break;
-
-    case 2:
-      if (c != VER_LEN) {
-        //result = ERROR_INVALID_VALUE;
-        parser_idx = 0;
-      }
-      break;
-
-    case 3: // speed LSB
-    case 4: // speed MSB
-      break;
-
-    case 5: // start angle LSB
-    case 6: // start angle MSB
-      break;
-
-    default:
-      if (parser_idx > 6 && parser_idx <= (6 + DATA_BYTE_LEN)) {
-      } else {
-        result = RESULT_INVALID_PACKET;
-      }
-      break;
-
-    case 43: // end angle LSB
-    case 44: // end angle MSB
-      break;
-
-    case 45: // timestamp LSB
-    case 46: // end angle MSB
-      break;
-
-    case 47: // CRC
-      if (crc != scan_packet.crc8) {
-        result = RESULT_CHECKSUM_ERROR;
-      } else {
-
-        speed_deg_per_sec = decodeUInt16(scan_packet.speed_deg_per_sec);
-        uint16_t start_angle_deg_x100 = decodeUInt16(scan_packet.start_angle_deg_x100);
-        uint16_t end_angle_deg_x100 = decodeUInt16(scan_packet.end_angle_deg_x100);
-
-        bool scan_completed_mid_packet = end_angle_deg_x100 < start_angle_deg_x100;
-        bool scan_completed_between_packets = start_angle_deg_x100 < end_angle_deg_x100_prev;
-        bool scan_completed = scan_completed_mid_packet || scan_completed_between_packets;
-        end_angle_deg_x100_prev = end_angle_deg_x100;
-
-        float start_angle = start_angle_deg_x100*0.01f;
-        float end_angle = end_angle_deg_x100*0.01f;
-
-        if (end_angle < start_angle)
-          end_angle = end_angle + 360;
-
-        static constexpr float _1_OVER_PPP_M1 = 1.0f / (POINTS_PER_PACK - 1);
-        float step_angle = (end_angle - start_angle)*_1_OVER_PPP_M1;
-
-        float angle_deg_prev = start_angle;
-        for (uint8_t i = 0; i < POINTS_PER_PACK; i++) {
-          float angle_deg = start_angle + step_angle * i;
-          float distance_mm = decodeUInt16(scan_packet.sample[i].distance_mm);
-          float quality = scan_packet.sample[i].intensity;
-
-          // no correction applied
-          float angle_corrected = angle_deg;
-
-          scan_completed = false;
-          if (scan_completed_mid_packet) {
-            scan_completed = (angle_deg >= 360 && angle_deg_prev < 360);
-          } else if (scan_completed_between_packets) {
-            scan_completed = (i == 0);
-          }
-
-          if (angle_corrected > 360)
-            angle_corrected -= 360;
-          if (angle_corrected < 0)
-            angle_corrected += 360;
-
-          postScanPoint(context, angle_corrected, distance_mm, quality, 0, scan_completed);
-          angle_deg_prev = angle_deg;
-        }
-      }
+        break;
+    }
+
+    if (result < RESULT_OK) {
@@ -209,5 +224 @@
-      break;
-    }
-
-    if (result < RESULT_OK)
-      parser_idx = 0;
+    }

Code style divergence in file 'src/lds_lds02rr.h':

--- src/lds_lds02rr.h
+++ src/lds_lds02rr.h.uncrustify
@@ -18 +18 @@
-class LDS_LDS02RR : public LDS_NeatoXV11
+class LDS_LDS02RR: public LDS_NeatoXV11
@@ -21,2 +21,3 @@
-  LDS_LDS02RR() : LDS_NeatoXV11() {}
-  static const std::string get_model_name() { return "XIAOMI-LDS02RR"; }
+  LDS_LDS02RR() : LDS_NeatoXV11() {
+  }
+  static const std::string get_model_name() {return "XIAOMI-LDS02RR";}

Code style divergence in file 'src/lds_neato_xv11.h':

--- src/lds_neato_xv11.h
+++ src/lds_neato_xv11.h.uncrustify
@@ -20 +20 @@
-class LDS_NeatoXV11 : public LDS
+class LDS_NeatoXV11: public LDS
@@ -23,52 +23,54 @@
-    // REF: https://github.com/Xevel/NXV11/wiki
-    // The bit 7 of byte 1 seems to indicate that the distance could not be calculated.
-    // It's interesting to see that when this bit is set, the second byte is always 80, and the values of the first byte seem to be
-    // only 02, 03, 21, 25, 35 or 50... When it's 21, then the whole block is 21 80 XX XX, but for all the other values it's the
-    // data block is YY 80 00 00 maybe it's a code to say what type of error ? (35 is preponderant, 21 seems to be when the beam is
-    // interrupted by the supports of the cover) .
-    static const uint8_t INVALID_DATA_FLAG = (1 << 7);   // Mask for byte 1 of each data quad "Invalid data"
-    // The bit 6 of byte 1 is a warning when the reported strength is greatly inferior to what is expected at this distance.
-    // This may happen when the material has a low reflectance (black material...), or when the dot does not have the expected
-    // size or shape (porous material, transparent fabric, grid, edge of an object...), or maybe when there are parasitic
-    // reflections (glass... ).
-    static const uint8_t STRENGTH_WARNING_FLAG = (1 << 6);  // Mask for byte 1 of each data quat "Strength Warning"
-    static const uint8_t CRC_ERROR_FLAG = (1 << 0);
-
-  protected:
-    int eState;
-    float scan_rpm;
-
-    static const unsigned char COMMAND = 0xFA;        // Start of new packet
-    static const int INDEX_LO = 0xA0;                 // lowest index value
-    static const int INDEX_HI = 0xF9;                 // highest index value
-
-    static const uint16_t N_DATA_QUADS = 4;                // there are 4 groups of data elements
-    static const uint16_t N_ELEMENTS_PER_QUAD = 4;         // viz., 0=distance LSB; 1=distance MSB; 2=sig LSB; 3=sig MSB
-
-    // Offsets to bytes within 'Packet'
-    static const uint16_t OFFSET_TO_START = 0;
-    static const uint16_t OFFSET_TO_INDEX = OFFSET_TO_START + 1;
-    static const uint16_t OFFSET_TO_SPEED_LSB = OFFSET_TO_INDEX + 1;
-    static const uint16_t OFFSET_TO_SPEED_MSB = OFFSET_TO_SPEED_LSB + 1;
-    static const uint16_t OFFSET_TO_4_DATA_READINGS = OFFSET_TO_SPEED_MSB + 1;
-    static const uint16_t OFFSET_TO_CRC_L = OFFSET_TO_4_DATA_READINGS + (N_DATA_QUADS * N_ELEMENTS_PER_QUAD);
-    static const uint16_t OFFSET_TO_CRC_M = OFFSET_TO_CRC_L + 1;
-    static const uint16_t PACKET_LENGTH = OFFSET_TO_CRC_M + 1;  // 22 length of a complete packet
-    // Offsets to the (4) elements of each of the (4) data quads
-    static const uint16_t OFFSET_DATA_DISTANCE_LSB = 0;
-    static const uint16_t OFFSET_DATA_DISTANCE_MSB = OFFSET_DATA_DISTANCE_LSB + 1;
-    static const uint16_t OFFSET_DATA_SIGNAL_LSB = OFFSET_DATA_DISTANCE_MSB + 1;
-    static const uint16_t OFFSET_DATA_SIGNAL_MSB = OFFSET_DATA_SIGNAL_LSB + 1;
-
-    uint8_t Packet[PACKET_LENGTH];                 // an input packet
-    uint16_t ixPacket;                          // index into 'Packet' array
-
-    static const uint8_t BAD_DATA_MASK = (INVALID_DATA_FLAG | STRENGTH_WARNING_FLAG);
-
-    enum e_state{
-      eState_Find_COMMAND = 0, // 1st state: find 0xFA (COMMAND) in input stream
-      eState_Build_Packet = 1, // 2nd state: build the packet
-    };
-    // static const uint8_t eState_Find_COMMAND = 0; // 1st state: find 0xFA (COMMAND) in input stream
-    // static const uint8_t eState_Build_Packet = eState_Find_COMMAND + 1;  // 2nd state: build the packet
-    uint16_t aryDist[N_DATA_QUADS];    // there are (4) distances, one for each data quad
+  // REF: https://github.com/Xevel/NXV11/wiki
+  // The bit 7 of byte 1 seems to indicate that the distance could not be calculated.
+  // It's interesting to see that when this bit is set, the second byte is always 80, and the values of the first byte seem to be
+  // only 02, 03, 21, 25, 35 or 50... When it's 21, then the whole block is 21 80 XX XX, but for all the other values it's the
+  // data block is YY 80 00 00 maybe it's a code to say what type of error ? (35 is preponderant, 21 seems to be when the beam is
+  // interrupted by the supports of the cover) .
+  static const uint8_t INVALID_DATA_FLAG = (1 << 7);     // Mask for byte 1 of each data quad "Invalid data"
+  // The bit 6 of byte 1 is a warning when the reported strength is greatly inferior to what is expected at this distance.
+  // This may happen when the material has a low reflectance (black material...), or when the dot does not have the expected
+  // size or shape (porous material, transparent fabric, grid, edge of an object...), or maybe when there are parasitic
+  // reflections (glass... ).
+  static const uint8_t STRENGTH_WARNING_FLAG = (1 << 6);    // Mask for byte 1 of each data quat "Strength Warning"
+  static const uint8_t CRC_ERROR_FLAG = (1 << 0);
+
+protected:
+  int eState;
+  float scan_rpm;
+
+  static const unsigned char COMMAND = 0xFA;          // Start of new packet
+  static const int INDEX_LO = 0xA0;                   // lowest index value
+  static const int INDEX_HI = 0xF9;                   // highest index value
+
+  static const uint16_t N_DATA_QUADS = 4;                  // there are 4 groups of data elements
+  static const uint16_t N_ELEMENTS_PER_QUAD = 4;           // viz., 0=distance LSB; 1=distance MSB; 2=sig LSB; 3=sig MSB
+
+  // Offsets to bytes within 'Packet'
+  static const uint16_t OFFSET_TO_START = 0;
+  static const uint16_t OFFSET_TO_INDEX = OFFSET_TO_START + 1;
+  static const uint16_t OFFSET_TO_SPEED_LSB = OFFSET_TO_INDEX + 1;
+  static const uint16_t OFFSET_TO_SPEED_MSB = OFFSET_TO_SPEED_LSB + 1;
+  static const uint16_t OFFSET_TO_4_DATA_READINGS = OFFSET_TO_SPEED_MSB + 1;
+  static const uint16_t OFFSET_TO_CRC_L = OFFSET_TO_4_DATA_READINGS +
+    (N_DATA_QUADS * N_ELEMENTS_PER_QUAD);
+  static const uint16_t OFFSET_TO_CRC_M = OFFSET_TO_CRC_L + 1;
+  static const uint16_t PACKET_LENGTH = OFFSET_TO_CRC_M + 1;    // 22 length of a complete packet
+  // Offsets to the (4) elements of each of the (4) data quads
+  static const uint16_t OFFSET_DATA_DISTANCE_LSB = 0;
+  static const uint16_t OFFSET_DATA_DISTANCE_MSB = OFFSET_DATA_DISTANCE_LSB + 1;
+  static const uint16_t OFFSET_DATA_SIGNAL_LSB = OFFSET_DATA_DISTANCE_MSB + 1;
+  static const uint16_t OFFSET_DATA_SIGNAL_MSB = OFFSET_DATA_SIGNAL_LSB + 1;
+
+  uint8_t Packet[PACKET_LENGTH];                   // an input packet
+  uint16_t ixPacket;                            // index into 'Packet' array
+
+  static const uint8_t BAD_DATA_MASK = (INVALID_DATA_FLAG | STRENGTH_WARNING_FLAG);
+
+  enum e_state
+  {
+    eState_Find_COMMAND = 0,   // 1st state: find 0xFA (COMMAND) in input stream
+    eState_Build_Packet = 1,   // 2nd state: build the packet
+  };
+  // static const uint8_t eState_Find_COMMAND = 0; // 1st state: find 0xFA (COMMAND) in input stream
+  // static const uint8_t eState_Build_Packet = eState_Find_COMMAND + 1;  // 2nd state: build the packet
+  uint16_t aryDist[N_DATA_QUADS];      // there are (4) distances, one for each data quad
@@ -76 +78 @@
-    uint16_t aryQuality[N_DATA_QUADS]; // same with 'quality'
+  uint16_t aryQuality[N_DATA_QUADS];   // same with 'quality'
@@ -85,4 +87,5 @@
-  float get_scan_time() override {
-    return (scan_rpm > 0) ? 60/scan_rpm : -1;
-  }
-  static const std::string get_model_name() { return "NEATO-XV11"; }
+  float get_scan_time() override
+  {
+    return (scan_rpm > 0) ? 60 / scan_rpm : -1;
+  }
+  static const std::string get_model_name() {return "NEATO-XV11";}
@@ -102 +105 @@
-    for (ixPacket = 0; ixPacket < PACKET_LENGTH; ixPacket++)  // clear out this packet
+    for (ixPacket = 0; ixPacket < PACKET_LENGTH; ixPacket++) { // clear out this packet
@@ -103,0 +107 @@
+    }
@@ -119 +123 @@
-    for (int ix = 0; ix < CalcCRC_Len; ix++)
+    for (int ix = 0; ix < CalcCRC_Len; ix++) {
@@ -120,0 +125 @@
+    }
@@ -123 +128 @@
-    for (ix = 0; ix < bytesToCheck; ix += 2)
+    for (ix = 0; ix < bytesToCheck; ix += 2) {
@@ -124,0 +130 @@
+    }
@@ -127 +133 @@
-    for (ix = 0; ix < CalcCRC_Len; ix++)
+    for (ix = 0; ix < CalcCRC_Len; ix++) {
@@ -128,0 +135 @@
+    }
@@ -136 +143 @@
-    return ((b1a == b1b) && (b2a == b2b));
+    return (b1a == b1b) && (b2a == b2b);
@@ -164 +171 @@
-    if (dataM & BAD_DATA_MASK)             // if either INVALID_DATA_FLAG or STRENGTH_WARNING_FLAG is set...
+    if (dataM & BAD_DATA_MASK) {           // if either INVALID_DATA_FLAG or STRENGTH_WARNING_FLAG is set...
@@ -165,0 +173 @@
+    }
@@ -176 +184 @@
-    scan_rpm = float( (scan_rph_high_byte << 8) | scan_rph_low_byte ) / 64.0;
+    scan_rpm = float( (scan_rph_high_byte << 8) | scan_rph_low_byte) / 64.0;
@@ -192 +200 @@
-  LDS::result_t processByte(const void *context, int inByte)
+  LDS::result_t processByte(const void * context, int inByte)
@@ -217 +225 @@
-          for (int ix = 0; ix < N_DATA_QUADS; ix++)   // process the distance
+          for (int ix = 0; ix < N_DATA_QUADS; ix++) { // process the distance
@@ -218,0 +227 @@
+          }
@@ -221 +230 @@
-            if (aryInvalidDataFlag[ix] == 0)
+            if (aryInvalidDataFlag[ix] == 0) {
@@ -222,0 +232 @@
+            }

Code style divergence in file 'src/lds_rplidar_a1.h':

--- src/lds_rplidar_a1.h
+++ src/lds_rplidar_a1.h.uncrustify
@@ -20 +20 @@
-class LDS_RPLidarA1 : public LDS
+class LDS_RPLidarA1: public LDS
@@ -23,4 +23,5 @@
-  struct node_info_t {
-    uint8_t    sync_quality;
-    uint16_t   angle_q6_checkbit;
-    uint16_t   distance_q2;
+  struct node_info_t
+  {
+    uint8_t sync_quality;
+    uint16_t angle_q6_checkbit;
+    uint16_t distance_q2;
@@ -32,2 +33,2 @@
-  static uint8_t const RESP_MEAS_CHECKBIT = (0x1<<0);
-  static uint8_t const RESP_MEAS_SYNCBIT = (0x1<<0);
+  static uint8_t const RESP_MEAS_CHECKBIT = (0x1 << 0);
+  static uint8_t const RESP_MEAS_SYNCBIT = (0x1 << 0);
@@ -43 +44 @@
-  static const std::string get_model_name() { return "SLAMTEC-RPLIDAR-A1"; }
+  static const std::string get_model_name() {return "SLAMTEC-RPLIDAR-A1";}
@@ -45 +46 @@
-  virtual float get_scan_time() override { return -1; }
+  virtual float get_scan_time() override {return -1;}
@@ -50 +51 @@
-      if (recvPos >= sizeof(node_info_t))
+      if (recvPos >= sizeof(node_info_t)) {
@@ -51,0 +53 @@
+      }
@@ -54 +56 @@
-      if (current_byte < 0)
+      if (current_byte < 0) {
@@ -55,0 +58 @@
+      }
@@ -58,11 +61,14 @@
-      case 0:
-        if (((current_byte >> 1) ^ current_byte) & 0x01)
-          break;
-        continue;
-      case 1:
-        if (current_byte & RESP_MEAS_CHECKBIT)
-          break;
-        recvPos = 0;
-        if (((current_byte >> 1) ^ current_byte) & 0x01)
-          break;
-        continue;
+        case 0:
+          if (((current_byte >> 1) ^ current_byte) & 0x01) {
+            break;
+          }
+          continue;
+        case 1:
+          if (current_byte & RESP_MEAS_CHECKBIT) {
+            break;
+          }
+          recvPos = 0;
+          if (((current_byte >> 1) ^ current_byte) & 0x01) {
+            break;
+          }
+          continue;
@@ -71 +77 @@
-      uint8_t *nodebuf = (uint8_t*)&node;
+      uint8_t * nodebuf = (uint8_t *)&node;

Code style divergence in file 'src/lds_ydlidar_scl.h':

--- src/lds_ydlidar_scl.h
+++ src/lds_ydlidar_scl.h.uncrustify
@@ -20 +20 @@
-class LDS_YDLidarSCL : public LDS
+class LDS_YDLidarSCL: public LDS
@@ -26 +26,2 @@
-  struct node_info_scl_t {
+  struct node_info_scl_t
+  {
@@ -33 +34,2 @@
-  struct cloud_point_scl_t {
+  struct cloud_point_scl_t
+  {
@@ -39,8 +41,9 @@
-  struct node_package_scl_t {
-    uint16_t  package_Head;
-    uint8_t   package_CT; // package type
-    uint8_t   nowPackageNum; // distance sample count
-    uint16_t  packageFirstSampleAngle;
-    uint16_t  packageLastSampleAngle;
-    uint16_t  checkSum;
-    cloud_point_scl_t  packageSampleDistance[PACKAGE_SAMPLE_MAX_LENGTH];
+  struct node_package_scl_t
+  {
+    uint16_t package_Head;
+    uint8_t package_CT;   // package type
+    uint8_t nowPackageNum;   // distance sample count
+    uint16_t packageFirstSampleAngle;
+    uint16_t packageLastSampleAngle;
+    uint16_t checkSum;
+    cloud_point_scl_t packageSampleDistance[PACKAGE_SAMPLE_MAX_LENGTH];
@@ -57 +60 @@
-  uint8_t *packageBuffer;
+  uint8_t * packageBuffer;
@@ -84 +87 @@
-    packageBuffer = (uint8_t*) &package_scl.package_Head;
+    packageBuffer = (uint8_t *) &package_scl.package_Head;
@@ -103,4 +106,5 @@
-  static const std::string get_model_name() { return "YDLIDAR-SCL"; }
-
-  virtual float get_scan_time() override {
-    return (scan_freq > 0) ? 10.0f/float(scan_freq) : -1;
+  static const std::string get_model_name() {return "YDLIDAR-SCL";}
+
+  virtual float get_scan_time() override
+  {
+    return (scan_freq > 0) ? 10.0f / float(scan_freq) : -1;
@@ -111 +115 @@
-    switch(state) {
+    switch (state) {
@@ -128 +132 @@
-state1:  // hack
+state1: // hack
@@ -137,50 +141,58 @@
-        case 0:
-          if (currentByte != 0xAA) { // (PH&0xFF)
-            continue;
-          }
-          break;
-        case 1:
-          CheckSumCal = 0x55AA; // PH
-          if (currentByte != 0x55) { // (PH>>8)
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 2:
-          SampleNumlAndCTCal = currentByte;
-          break;
-        case 3:
-          SampleNumlAndCTCal += (currentByte << 8);
-          package_Sample_Num = currentByte;
-          break;
-        case 4:
-          if (currentByte & 0x01) {
-            FirstSampleAngle = currentByte;
-          } else {
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 5:
-          FirstSampleAngle += (currentByte << 8);
-          CheckSumCal ^= FirstSampleAngle;
-          FirstSampleAngle = FirstSampleAngle >> 1; // degrees*64
-          break;
-        case 6:
-          if (currentByte & 0x01) {
-            LastSampleAngle = currentByte;
-          } else {
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 7:
-          LastSampleAngle += (currentByte << 8);
-          LastSampleAngleCal = LastSampleAngle;
-          LastSampleAngle = LastSampleAngle >> 1;
-          if (package_Sample_Num == 1) {
-            IntervalSampleAngle = 0;
-          } else {
-            if (LastSampleAngle < FirstSampleAngle) {
-              if ((FirstSampleAngle > 270*64) && (LastSampleAngle < 90*64)){
-                IntervalSampleAngle = ((float)(360*64 + LastSampleAngle - FirstSampleAngle))/(package_Sample_Num-1);
+          case 0:
+            if (currentByte != 0xAA) { // (PH&0xFF)
+              continue;
+            }
+            break;
+          case 1:
+            CheckSumCal = 0x55AA; // PH
+            if (currentByte != 0x55) { // (PH>>8)
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 2:
+            SampleNumlAndCTCal = currentByte;
+            break;
+          case 3:
+            SampleNumlAndCTCal += (currentByte << 8);
+            package_Sample_Num = currentByte;
+            break;
+          case 4:
+            if (currentByte & 0x01) {
+              FirstSampleAngle = currentByte;
+            } else {
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 5:
+            FirstSampleAngle += (currentByte << 8);
+            CheckSumCal ^= FirstSampleAngle;
+            FirstSampleAngle = FirstSampleAngle >> 1; // degrees*64
+            break;
+          case 6:
+            if (currentByte & 0x01) {
+              LastSampleAngle = currentByte;
+            } else {
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 7:
+            LastSampleAngle += (currentByte << 8);
+            LastSampleAngleCal = LastSampleAngle;
+            LastSampleAngle = LastSampleAngle >> 1;
+            if (package_Sample_Num == 1) {
+              IntervalSampleAngle = 0;
+            } else {
+              if (LastSampleAngle < FirstSampleAngle) {
+                if ((FirstSampleAngle > 270 * 64) && (LastSampleAngle < 90 * 64)) {
+                  IntervalSampleAngle = ((float)(360 * 64 + LastSampleAngle - FirstSampleAngle)) /
+                    (package_Sample_Num - 1);
+                  IntervalSampleAngle_LastPackage = IntervalSampleAngle;
+                } else {
+                  IntervalSampleAngle = IntervalSampleAngle_LastPackage;
+                }
+              } else {
+                IntervalSampleAngle = ((float)(LastSampleAngle - FirstSampleAngle)) /
+                  (package_Sample_Num - 1);
@@ -188,2 +199,0 @@
-              } else{
-                IntervalSampleAngle = IntervalSampleAngle_LastPackage;
@@ -191,12 +201,8 @@
-            } else {
-              IntervalSampleAngle = ((float)(LastSampleAngle - FirstSampleAngle))/(package_Sample_Num-1);
-              IntervalSampleAngle_LastPackage = IntervalSampleAngle;
-            }
-          }
-          break;
-        case 8:
-          CheckSum = currentByte;
-          break;
-        case 9:
-          CheckSum += (currentByte << 8);
-          break;
+            }
+            break;
+          case 8:
+            CheckSum = currentByte;
+            break;
+          case 9:
+            CheckSum += (currentByte << 8);
+            break;
@@ -206 +212 @@
-        if (recvPos  == PACKAGE_PAID_BYTES) {
+        if (recvPos == PACKAGE_PAID_BYTES) {
@@ -213 +219 @@
-      if (package_Sample_Num > PACKAGE_SAMPLE_MAX_LENGTH)
+      if (package_Sample_Num > PACKAGE_SAMPLE_MAX_LENGTH) {
@@ -214,0 +221 @@
+      }
@@ -266 +273 @@
-      if (scan_completed)
+      if (scan_completed) {
@@ -267,0 +275 @@
+      }
@@ -270 +278 @@
-    while(true) {
+    while (true) {
@@ -283 +291,2 @@
-          AngleCorrectForDistance = (int32_t)(atan(17.8f/((float)node.distance_mm))*3666.929888837269f);
+          AngleCorrectForDistance =
+            (int32_t)(atan(17.8f / ((float)node.distance_mm)) * 3666.929888837269f);
@@ -308 +317,3 @@
-      postScanPoint(context, node.angle_deg, node.distance_mm, node.quality_flag, node.intensity, scan_completed);
+      postScanPoint(
+        context, node.angle_deg, node.distance_mm, node.quality_flag, node.intensity,
+        scan_completed);

Code style divergence in file 'src/lds_ydlidar_x2_x2l.h':

--- src/lds_ydlidar_x2_x2l.h
+++ src/lds_ydlidar_x2_x2l.h.uncrustify
@@ -18 +18 @@
-class LDS_YDLidarX2X2L : public LDS_YDLidarX4
+class LDS_YDLidarX2X2L: public LDS_YDLidarX4
@@ -21,3 +21 @@
-  LDS_YDLidarX2X2L() : LDS_YDLidarX4() {}
-  virtual float get_scan_time() override {
-    return (scan_freq > 0) ? 10.0f/float(scan_freq) : -1;
+  LDS_YDLidarX2X2L() : LDS_YDLidarX4() {
@@ -25 +23,5 @@
-  static const std::string get_model_name() { return "YDLIDAR-X2-X2L"; }
+  virtual float get_scan_time() override
+  {
+    return (scan_freq > 0) ? 10.0f / float(scan_freq) : -1;
+  }
+  static const std::string get_model_name() {return "YDLIDAR-X2-X2L";}

Code style divergence in file 'src/lds_ydlidar_x3.h':

--- src/lds_ydlidar_x3.h
+++ src/lds_ydlidar_x3.h.uncrustify
@@ -18 +18 @@
-class LDS_YDLidarX3 : public LDS_YDLidarX2X2L
+class LDS_YDLidarX3: public LDS_YDLidarX2X2L
@@ -21,2 +21,3 @@
-  LDS_YDLidarX3() : LDS_YDLidarX2X2L() {}
-  static const std::string get_model_name() { return "YDLIDAR-X3"; }
+  LDS_YDLidarX3() : LDS_YDLidarX2X2L() {
+  }
+  static const std::string get_model_name() {return "YDLIDAR-X3";}

Code style divergence in file 'src/lds_ydlidar_x3_pro.h':

--- src/lds_ydlidar_x3_pro.h
+++ src/lds_ydlidar_x3_pro.h.uncrustify
@@ -18 +18 @@
-class LDS_YDLidarX3PRO : public LDS_YDLidarX2X2L
+class LDS_YDLidarX3PRO: public LDS_YDLidarX2X2L
@@ -21,2 +21,3 @@
-  LDS_YDLidarX3PRO() : LDS_YDLidarX2X2L() {}
-  static const std::string get_model_name() { return "YDLIDAR-X3-PRO"; }
+  LDS_YDLidarX3PRO() : LDS_YDLidarX2X2L() {
+  }
+  static const std::string get_model_name() {return "YDLIDAR-X3-PRO";}

Code style divergence in file 'src/lds_ydlidar_x4.h':

--- src/lds_ydlidar_x4.h
+++ src/lds_ydlidar_x4.h.uncrustify
@@ -20 +20 @@
-class LDS_YDLidarX4 : public LDS
+class LDS_YDLidarX4: public LDS
@@ -23,2 +23,2 @@
-  static const int RESP_MEASUREMENT_SYNCBIT = (0x1<<0);
-  static const int RESP_MEASUREMENT_CHECKBIT = (0x1<<0);
+  static const int RESP_MEASUREMENT_SYNCBIT = (0x1 << 0);
+  static const int RESP_MEASUREMENT_CHECKBIT = (0x1 << 0);
@@ -36 +36,2 @@
-  enum {
+  enum
+  {
@@ -42,4 +43,5 @@
-  struct node_info {
-    uint8_t    sync_quality;
-    uint16_t   angle_q6_checkbit;
-    uint16_t   distance_q2;
+  struct node_info
+  {
+    uint8_t sync_quality;
+    uint16_t angle_q6_checkbit;
+    uint16_t distance_q2;
@@ -48,8 +50,9 @@
-  struct node_package {
-    uint16_t  package_Head;
-    uint8_t   package_CT;
-    uint8_t   nowPackageNum;
-    uint16_t  packageFirstSampleAngle;
-    uint16_t  packageLastSampleAngle;
-    uint16_t  checkSum;
-    uint16_t  packageSampleDistance[PACKAGE_SAMPLE_MAX_LENGTH];
+  struct node_package
+  {
+    uint16_t package_Head;
+    uint8_t package_CT;
+    uint8_t nowPackageNum;
+    uint16_t packageFirstSampleAngle;
+    uint16_t packageLastSampleAngle;
+    uint16_t checkSum;
+    uint16_t packageSampleDistance[PACKAGE_SAMPLE_MAX_LENGTH];
@@ -66 +69 @@
-  uint8_t *packageBuffer;
+  uint8_t * packageBuffer;
@@ -93 +96 @@
-    packageBuffer = (uint8_t*) &package.package_Head;
+    packageBuffer = (uint8_t *) &package.package_Head;
@@ -112,3 +115,3 @@
-  static const std::string get_model_name() { return "YDLIDAR-X4"; }
-
-  virtual float get_scan_time() override { return -1; }
+  static const std::string get_model_name() {return "YDLIDAR-X4";}
+
+  virtual float get_scan_time() override {return -1;}
@@ -118 +121 @@
-    switch(state) {
+    switch (state) {
@@ -135 +138 @@
-state1:  // hack
+state1: // hack
@@ -144,50 +147,58 @@
-        case 0:
-          if (currentByte != (PH&0xFF)) {
-            continue;
-          }
-          break;
-        case 1:
-          CheckSumCal = PH;
-          if (currentByte!=(PH>>8)) {
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 2:
-          SampleNumlAndCTCal = currentByte;
-          break;
-        case 3:
-          SampleNumlAndCTCal += (currentByte<<RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
-          package_Sample_Num = currentByte;
-          break;
-        case 4:
-          if (currentByte & RESP_MEASUREMENT_CHECKBIT) {
-            FirstSampleAngle = currentByte;
-          } else {
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 5:
-          FirstSampleAngle += (currentByte<<RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
-          CheckSumCal ^= FirstSampleAngle;
-          FirstSampleAngle = FirstSampleAngle>>1;
-          break;
-        case 6:
-          if (currentByte & RESP_MEASUREMENT_CHECKBIT) {
-            LastSampleAngle = currentByte;
-          } else {
-            recvPos = 0;
-            continue;
-          }
-          break;
-        case 7:
-          LastSampleAngle += (currentByte<<RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
-          LastSampleAngleCal = LastSampleAngle;
-          LastSampleAngle = LastSampleAngle>>1;
-          if(package_Sample_Num == 1){
-            IntervalSampleAngle = 0;
-          }else{
-            if(LastSampleAngle < FirstSampleAngle){
-              if((FirstSampleAngle > 17280) && (LastSampleAngle < 5760)){
-                IntervalSampleAngle = ((float)(23040 + LastSampleAngle - FirstSampleAngle))/(package_Sample_Num-1);
+          case 0:
+            if (currentByte != (PH & 0xFF)) {
+              continue;
+            }
+            break;
+          case 1:
+            CheckSumCal = PH;
+            if (currentByte != (PH >> 8)) {
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 2:
+            SampleNumlAndCTCal = currentByte;
+            break;
+          case 3:
+            SampleNumlAndCTCal += (currentByte << RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
+            package_Sample_Num = currentByte;
+            break;
+          case 4:
+            if (currentByte & RESP_MEASUREMENT_CHECKBIT) {
+              FirstSampleAngle = currentByte;
+            } else {
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 5:
+            FirstSampleAngle += (currentByte << RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
+            CheckSumCal ^= FirstSampleAngle;
+            FirstSampleAngle = FirstSampleAngle >> 1;
+            break;
+          case 6:
+            if (currentByte & RESP_MEASUREMENT_CHECKBIT) {
+              LastSampleAngle = currentByte;
+            } else {
+              recvPos = 0;
+              continue;
+            }
+            break;
+          case 7:
+            LastSampleAngle += (currentByte << RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
+            LastSampleAngleCal = LastSampleAngle;
+            LastSampleAngle = LastSampleAngle >> 1;
+            if (package_Sample_Num == 1) {
+              IntervalSampleAngle = 0;
+            } else {
+              if (LastSampleAngle < FirstSampleAngle) {
+                if ((FirstSampleAngle > 17280) && (LastSampleAngle < 5760)) {
+                  IntervalSampleAngle = ((float)(23040 + LastSampleAngle - FirstSampleAngle)) /
+                    (package_Sample_Num - 1);
+                  IntervalSampleAngle_LastPackage = IntervalSampleAngle;
+                } else {
+                  IntervalSampleAngle = IntervalSampleAngle_LastPackage;
+                }
+              } else {
+                IntervalSampleAngle = ((float)(LastSampleAngle - FirstSampleAngle)) /
+                  (package_Sample_Num - 1);
@@ -195,2 +205,0 @@
-              } else{
-                IntervalSampleAngle = IntervalSampleAngle_LastPackage;
@@ -198,12 +207,8 @@
-            } else{
-              IntervalSampleAngle = ((float)(LastSampleAngle -FirstSampleAngle))/(package_Sample_Num-1);
-              IntervalSampleAngle_LastPackage = IntervalSampleAngle;
-            }
-          }
-          break;
-        case 8:
-          CheckSum = currentByte;
-          break;
-        case 9:
-          CheckSum += (currentByte<<RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
-          break;
+            }
+            break;
+          case 8:
+            CheckSum = currentByte;
+            break;
+          case 9:
+            CheckSum += (currentByte << RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
+            break;
@@ -213 +218 @@
-        if (recvPos  == PACKAGE_PAID_BYTES ){
+        if (recvPos == PACKAGE_PAID_BYTES) {
@@ -221 +226 @@
-      if (package_Sample_Num > PACKAGE_SAMPLE_MAX_LENGTH)
+      if (package_Sample_Num > PACKAGE_SAMPLE_MAX_LENGTH) {
@@ -222,0 +228 @@
+      }
@@ -227 +233 @@
-        package_sample_sum = package_Sample_Num<<1;
+        package_sample_sum = package_Sample_Num << 1;
@@ -237 +243 @@
-            Valu8Tou16 += (currentByte<<RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
+            Valu8Tou16 += (currentByte << RESP_MEASUREMENT_ANGLE_SAMPLE_SHIFT);
@@ -243 +249 @@
-          packageBuffer[package_recvPos+recvPos] = currentByte;
+          packageBuffer[package_recvPos + recvPos] = currentByte;
@@ -245 +251 @@
-          if(package_sample_sum == recvPos){
+          if (package_sample_sum == recvPos) {
@@ -268 +274 @@
-      if (scan_completed)
+      if (scan_completed) {
@@ -269,0 +276 @@
+      }
@@ -272 +279 @@
-    while(true) {
+    while (true) {
@@ -282,3 +289,4 @@
-        if (node.distance_q2/4 != 0) {
-          AngleCorrectForDistance = (int32_t)((atan(((21.8*(155.3 -
-            (node.distance_q2*0.25f)) )/155.3)/(node.distance_q2*0.25f)))*3666.93);
+        if (node.distance_q2 / 4 != 0) {
+          AngleCorrectForDistance = (int32_t)((atan(
+              ((21.8 * (155.3 -
+              (node.distance_q2 * 0.25f)) ) / 155.3) / (node.distance_q2 * 0.25f))) * 3666.93);
@@ -288 +296 @@
-        float sampleAngle = IntervalSampleAngle*package_Sample_Index;
+        float sampleAngle = IntervalSampleAngle * package_Sample_Index;
@@ -291 +299,2 @@
-            AngleCorrectForDistance + 23040))<<RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
+            AngleCorrectForDistance + 23040)) <<
+            RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
@@ -295 +304,2 @@
-              AngleCorrectForDistance - 23040))<<RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
+              AngleCorrectForDistance - 23040)) <<
+              RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
@@ -298 +308,2 @@
-              AngleCorrectForDistance))<<RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
+              AngleCorrectForDistance)) <<
+              RESP_MEASUREMENT_ANGLE_SHIFT) + RESP_MEASUREMENT_CHECKBIT;
@@ -310,2 +321,2 @@
-      float point_distance_mm = node.distance_q2*0.25f;
-      float point_angle_deg = (node.angle_q6_checkbit >> RESP_MEASUREMENT_ANGLE_SHIFT)/64.0f;
+      float point_distance_mm = node.distance_q2 * 0.25f;
+      float point_angle_deg = (node.angle_q6_checkbit >> RESP_MEASUREMENT_ANGLE_SHIFT) / 64.0f;

Code style divergence in file 'src/telem.cpp':

--- src/telem.cpp
+++ src/telem.cpp.uncrustify
@@ -59 +59,3 @@
-    this->declare_parameter("lidar.model", std::vector<std::string>({"YDLIDAR-X4", "LDROBOT-LD19"}));
+    this->declare_parameter(
+      "lidar.model",
+      std::vector<std::string>({"YDLIDAR-X4", "LDROBOT-LD19"}));
@@ -88,2 +90,2 @@
-    this->declare_parameter("battery.voltage_full", 4.2*6);
-    this->declare_parameter("battery.voltage_empty", 3.7*6);
+    this->declare_parameter("battery.voltage_full", 4.2 * 6);
+    this->declare_parameter("battery.voltage_empty", 3.7 * 6);
@@ -115 +117 @@
-    min_scan_time_ = 1/4.0;
+    min_scan_time_ = 1 / 4.0;
@@ -145 +147 @@
-      RCLCPP_INFO(this->get_logger(), "%ld message(s) lost", seq_diff-1);
+      RCLCPP_INFO(this->get_logger(), "%ld message(s) lost", seq_diff - 1);
@@ -212 +214 @@
-      if (plds == NULL)
+      if (plds == NULL) {
@@ -213,0 +216 @@
+      }
@@ -230 +233,2 @@
-      RCLCPP_FATAL(this->get_logger(),
+      RCLCPP_FATAL(
+        this->get_logger(),
@@ -235 +239 @@
-    double percentage = (voltage - voltage_empty)/(voltage_full - voltage_empty) * 100;
+    double percentage = (voltage - voltage_empty) / (voltage_full - voltage_empty) * 100;
@@ -250,2 +254,3 @@
-    if (plds != NULL)
-      return;
+    if (plds != NULL) {
+      return;
+    }
@@ -254 +259,2 @@
-    const std::vector<double> angle_offset_deg = this->get_parameter("lidar.angle_offset_deg").as_double_array();
+    const std::vector<double> angle_offset_deg =
+      this->get_parameter("lidar.angle_offset_deg").as_double_array();
@@ -256,4 +262,8 @@
-    const std::vector<long int> pub_scan_size = this->get_parameter("lidar.pub_scan_size").as_integer_array();
-    const std::vector<double> range_min_meters = this->get_parameter("lidar.range_min_meters").as_double_array();
-    const std::vector<double> range_max_meters = this->get_parameter("lidar.range_max_meters").as_double_array();
-    const std::vector<bool> publish_intensity = this->get_parameter("lidar.intensity").as_bool_array();
+    const std::vector<long int> pub_scan_size =
+      this->get_parameter("lidar.pub_scan_size").as_integer_array();
+    const std::vector<double> range_min_meters =
+      this->get_parameter("lidar.range_min_meters").as_double_array();
+    const std::vector<double> range_max_meters =
+      this->get_parameter("lidar.range_max_meters").as_double_array();
+    const std::vector<bool> publish_intensity =
+      this->get_parameter("lidar.intensity").as_bool_array();
@@ -262,3 +272,4 @@
-    if (pub_scan_size.size() != model_count || angle_offset_deg.size() != model_count
-        || range_min_meters.size() != model_count || range_max_meters.size() != model_count
-        || clockwise.size() != model_count || publish_intensity.size() != model_count) {
+    if (pub_scan_size.size() != model_count || angle_offset_deg.size() != model_count ||
+      range_min_meters.size() != model_count || range_max_meters.size() != model_count ||
+      clockwise.size() != model_count || publish_intensity.size() != model_count)
+    {
@@ -272 +283 @@
-    for (auto &s: model) {
+    for (auto & s: model) {
@@ -274,30 +285,30 @@
-      if (s == LDS_YDLidarX3::get_model_name()) {
-        plds = new LDS_YDLidarX3();
-      } else if (s == LDS_YDLidarX2X2L::get_model_name()) {
-        plds = new LDS_YDLidarX2X2L();
-      } else if (s == LDS_LDS02RR::get_model_name()) {
-        plds = new LDS_LDS02RR();
-      } else if (s == LDS_NeatoXV11::get_model_name()) {
-        plds = new LDS_NeatoXV11();
-      } else if (s == LDS_RPLidarA1::get_model_name()) {
-        plds = new LDS_RPLidarA1();
-      } else if (s == LDS_YDLidarX3PRO::get_model_name()) {
-        plds = new LDS_YDLidarX3PRO();
-      } else if (s == LDS_Delta2G::get_model_name()) {
-        plds = new LDS_Delta2G();
-      } else if (s == LDS_Delta2A::get_model_name()) {
-        plds = new LDS_Delta2A();
-      } else if (s == LDS_Delta2B::get_model_name()) {
-        plds = new LDS_Delta2B();
-      } else if (s == LDS_LDRobotLD14P::get_model_name()) {
-        plds = new LDS_LDRobotLD14P();
-      } else if (s == LDS_LDRobotLD19::get_model_name()) {
-        plds = new LDS_LDRobotLD19();        
-      } else if (s == LDS_YDLidarX4::get_model_name()) {
-        plds = new LDS_YDLidarX4();
-      } else if (s == LDS_YDLidarSCL::get_model_name()) {
-        plds = new LDS_YDLidarSCL();
-      } else if (s == LDS_CamsenseX1::get_model_name()) {
-        plds = new LDS_CamsenseX1();
-      }
-      if (plds != NULL) break;
+        if (s == LDS_YDLidarX3::get_model_name()) {
+          plds = new LDS_YDLidarX3();
+        } else if (s == LDS_YDLidarX2X2L::get_model_name()) {
+          plds = new LDS_YDLidarX2X2L();
+        } else if (s == LDS_LDS02RR::get_model_name()) {
+          plds = new LDS_LDS02RR();
+        } else if (s == LDS_NeatoXV11::get_model_name()) {
+          plds = new LDS_NeatoXV11();
+        } else if (s == LDS_RPLidarA1::get_model_name()) {
+          plds = new LDS_RPLidarA1();
+        } else if (s == LDS_YDLidarX3PRO::get_model_name()) {
+          plds = new LDS_YDLidarX3PRO();
+        } else if (s == LDS_Delta2G::get_model_name()) {
+          plds = new LDS_Delta2G();
+        } else if (s == LDS_Delta2A::get_model_name()) {
+          plds = new LDS_Delta2A();
+        } else if (s == LDS_Delta2B::get_model_name()) {
+          plds = new LDS_Delta2B();
+        } else if (s == LDS_LDRobotLD14P::get_model_name()) {
+          plds = new LDS_LDRobotLD14P();
+        } else if (s == LDS_LDRobotLD19::get_model_name()) {
+          plds = new LDS_LDRobotLD19();
+        } else if (s == LDS_YDLidarX4::get_model_name()) {
+          plds = new LDS_YDLidarX4();
+        } else if (s == LDS_YDLidarSCL::get_model_name()) {
+          plds = new LDS_YDLidarSCL();
+        } else if (s == LDS_CamsenseX1::get_model_name()) {
+          plds = new LDS_CamsenseX1();
+        }
+        if (plds != NULL) {break;}
@@ -342,2 +353,3 @@
-    if (plds == NULL)
-      return;
+    if (plds == NULL) {
+      return;
+    }
@@ -349 +361 @@
-    pmsg = const_cast<kaiaai_msgs::msg::KaiaaiTelemetry2 *>(& telem_msg);
+    pmsg = const_cast<kaiaai_msgs::msg::KaiaaiTelemetry2 *>(&telem_msg);
@@ -354 +366 @@
-      switch(err) {
+      switch (err) {
@@ -376 +388 @@
-    if ((pmsg == NULL) || (lds_data_idx_ >= pmsg->lds.size()))
+    if ((pmsg == NULL) || (lds_data_idx_ >= pmsg->lds.size())) {
@@ -377,0 +390 @@
+    }
@@ -386,4 +399,5 @@
-    return reinterpret_cast<KaiaaiTelemetry*>(ctx)->read_lds_byte();
-  }
-
-  static void scan_point_callback(const void * context,
+    return reinterpret_cast<KaiaaiTelemetry *>(ctx)->read_lds_byte();
+  }
+
+  static void scan_point_callback(
+    const void * context,
@@ -393 +407 @@
-    return reinterpret_cast<KaiaaiTelemetry*>(ctx)->process_scan_point(
+    return reinterpret_cast<KaiaaiTelemetry *>(ctx)->process_scan_point(
@@ -397 +411,3 @@
-  void process_scan_point(float angle_deg, float distance_mm, float quality, float intensity, bool scan_completed)
+  void process_scan_point(
+    float angle_deg, float distance_mm, float quality, float intensity,
+    bool scan_completed)
@@ -410,7 +426,9 @@
-    if (distance_mm == 0.0)  // Invalid measurement
-      return;
-
-
-    double distance_meters = distance_mm*0.001;
-    if (mask_radius_meters_ >= distance_meters) // Ignore
-      return;
+    if (distance_mm == 0.0) { // Invalid measurement
+      return;
+    }
+
+
+    double distance_meters = distance_mm * 0.001;
+    if (mask_radius_meters_ >= distance_meters) { // Ignore
+      return;
+    }
@@ -425,2 +443 @@
-    if (idx >= 0 && idx < ((long int)ranges_.size()))
-    {
+    if (idx >= 0 && idx < ((long int)ranges_.size())) {
@@ -474 +491,2 @@
-    laser_scan_msg.angle_max = (angle_offset_deg_ + 360.0 * (pub_scan_size_ - 1) / pub_scan_size_) * DEG_TO_RAD;
+    laser_scan_msg.angle_max = (angle_offset_deg_ + 360.0 * (pub_scan_size_ - 1) / pub_scan_size_) *
+      DEG_TO_RAD;
@@ -478 +496 @@
-    if (publish_intensity_)
+    if (publish_intensity_) {
@@ -479,0 +498 @@
+    }
@@ -482 +501,2 @@
-    float scan_time = pmsg->stamp.sec - scan_start_stamp_.sec + (pmsg->stamp.nanosec - scan_start_stamp_.nanosec)*1e-9;
+    float scan_time = pmsg->stamp.sec - scan_start_stamp_.sec +
+      (pmsg->stamp.nanosec - scan_start_stamp_.nanosec) * 1e-9;
@@ -495 +515 @@
-    laser_scan_msg.time_increment = scan_time/(pub_scan_size_ - 1);
+    laser_scan_msg.time_increment = scan_time / (pub_scan_size_ - 1);

Code style divergence in file 'src/telem_test_pub.cpp':

--- src/telem_test_pub.cpp
+++ src/telem_test_pub.cpp.uncrustify
@@ -51 +51 @@
-    joint_pos[0] = float(count_)*0.1;
+    joint_pos[0] = float(count_) * 0.1;
@@ -57 +57 @@
-    joint_vel[0] = msg.odom_pos_x*0.01;
+    joint_vel[0] = msg.odom_pos_x * 0.01;
@@ -63 +63 @@
-    for (long unsigned int i = 0; i < lds.size(); i++)
+    for (long unsigned int i = 0; i < lds.size(); i++) {
@@ -64,0 +65 @@
+    }

17 files with code style divergence
-- run_test.py: return code 1
-- run_test.py: verify result file '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/uncrustify.xunit.xml'
<end of output>
Test time =   0.50 sec
----------------------------------------------------------
Test Failed.
"uncrustify" end time: Aug 01 02:04 +07
"uncrustify" time elapsed: 00:00:00
----------------------------------------------------------

4/4 Testing: xmllint
4/4 Test: xmllint
Command: "/usr/bin/python3.10" "-u" "/opt/ros/iron/share/ament_cmake_test/cmake/run_test.py" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/xmllint.xunit.xml" "--package-name" "kaiaai_telemetry" "--output-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/ament_xmllint/xmllint.txt" "--command" "/opt/ros/iron/bin/ament_xmllint" "--xunit-file" "/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/xmllint.xunit.xml"
Directory: /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry
"xmllint" start time: Aug 01 02:04 +07
Output:
----------------------------------------------------------
-- run_test.py: invoking following command in '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/src/kaiaai_telemetry':
 - /opt/ros/iron/bin/ament_xmllint --xunit-file /home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/xmllint.xunit.xml
File 'package.xml' is invalid:
warning: failed to load external entity "http://download.ros.org/schema/package_format3.xsd"
Schemas parser error : Failed to locate the main schema resource at 'http://download.ros.org/schema/package_format3.xsd'.
WXS schema http://download.ros.org/schema/package_format3.xsd failed to compile

1 files are invalid
-- run_test.py: return code 1
-- run_test.py: verify result file '/home/duong/Arduino/ROS2_ServiceRobot/kaia_ws/build/kaiaai_telemetry/test_results/kaiaai_telemetry/xmllint.xunit.xml'
<end of output>
Test time =   0.22 sec
----------------------------------------------------------
Test Failed.
"xmllint" end time: Aug 01 02:04 +07
"xmllint" time elapsed: 00:00:00
----------------------------------------------------------

End testing: Aug 01 02:04 +07

cppcheck =   0.28 sec*proc

lint_cmake =   0.24 sec*proc

linter =   1.24 sec*proc

uncrustify =   0.50 sec*proc

xmllint =   0.22 sec*proc

